# Memant node-vault marker. This repo's vault/ latches into the local hub via 'memant sync'.
name: clawdbot-railway-template
namespace: clawdbot-railway-template        # cross-vault links use [[clawdbot-railway-template/<page>]]
shareable: true         # ONLY shareable project knowledge here — it travels with the repo (git)
